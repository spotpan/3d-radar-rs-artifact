# 3DMAE 定量降水估计项目计划

## 项目概述
实现基于三维掩码自编码器（3DMAE）的定量降水估计方法，从三维雷达回波数据反演小时累积降水量。项目分为两阶段：
1. 3DMAE自监督预训练：使用无标签雷达数据学习通用三维时空特征
2. 降水反演微调：冻结预训练编码器，添加多帧时序融合和双头解码器进行有监督训练

## 数据
- 来源：`/mnt/md1/hxc/guangdong/train_select/` 下的HDF5文件
- 文件：`time_radar_rain_2022.h5`, `time_radar_rain_2023.h5`, `time_radar_rain_2025.h5`
- 结构：
  - `radar`: (N, 11, 700, 900) int8，11个高度层的雷达反射率
  - `rain`: (N, 700, 900) float16，插值降水网格（mm/h）
  - `radar_valid`, `rain_valid`: 有效性标记
  - `time`: 时间字符串（格式待确认）
- 预处理：
  - 使用前6个高度层（论文中1-6km）
  - 时间对齐：降水反演使用每小时00和30分的降水标签，对应6帧雷达数据（间隔12分钟）
  - 自监督预训练使用所有时次雷达数据

## 模型架构

### 1. 3DMAE预训练模型
- 编码器：
  - Patch嵌入：16×16空间块，每个块包含6个高度层（6×16×16 → 768维）
  - Transformer编码器：16层，768维，12头，MLP隐藏层3072
  - 掩码比例：80%，仅可见块输入编码器
- 解码器：
  - 输入：可见块特征 + 掩码令牌
  - Transformer解码器：8层，512维，8头，MLP隐藏层2048
  - 输出头：线性层重建每个块的6×16×16像素
- 预训练目标：重建被掩蔽的雷达像素值（MSE损失）

### 2. 降水反演模型
- 特征提取：冻结的3DMAE编码器（输出768维特征，2408个空间块）
- 多帧时序融合：
  - 输入：6帧连续雷达（间隔12分钟）
  - 每帧独立通过编码器得到特征
  - 添加可学习时间位置编码
  - 逐点时间Transformer：2层，8头，对每个空间位置独立建模时间依赖
  - 输出：融合后的单帧特征
- 双头解码器：
  - 上采样解码器：4阶段上采样（2倍×4），卷积+BN+ReLU
  - 回归头：1×1卷积输出降水强度（mm/h），最后30% epoch使用Softplus激活
  - 分类头：1×1卷积输出有雨概率，Sigmoid激活
- 输出：700×900降水强度图和有雨概率图

## 损失函数
- 回归损失（分位数损失，q=0.9）：
  - 全局网格损失：预测降水网格与插值网格之间的分位数损失
  - 站点邻域损失：以每个站点为中心的高斯加权邻域平均预测 vs 站点观测
- 分类损失：二元交叉熵（有雨阈值0.1 mm/h）
- 总损失：`L_total = L_reg_global + 0.5*L_reg_point + 0.3*L_cls`

## 实现步骤

### 阶段1：环境设置与数据探索
1. 创建Python虚拟环境，安装依赖（PyTorch, h5py, numpy等）
2. 详细分析HDF5文件结构，验证数据质量
3. 设计数据加载器，支持预训练和微调的不同采样策略

### 阶段2：3DMAE预训练实现
1. 实现Patch嵌入和位置编码
2. 实现3DMAE编码器（ViT架构）
3. 实现3DMAE解码器
4. 实现掩码生成和重建损失
5. 训练脚本，使用无标签雷达数据

### 阶段3：降水反演模型实现
1. 加载预训练编码器权重并冻结
2. 实现多帧时序融合模块（时间位置编码+Transformer）
3. 实现双头上采样解码器
4. 实现联合损失函数（分位数损失、站点邻域加权、BCE）
5. 训练脚本，使用雷达-降水配对数据

### 阶段4：训练与验证
1. 划分训练/验证/测试集（按时间）
2. 实现评估指标（MAE, RMSE, CSI, CC等）
3. 训练监控（TensorBoard/MLflow）
4. 模型保存与加载

### 阶段5：文档与部署
1. 编写使用说明
2. 创建推理脚本
3. 性能分析和可视化

## 目录结构
```
3DMAE/
├── data/
│   ├── dataloader.py
│   ├── preprocess.py
│   └── utils.py
├── models/
│   ├── mae.py          # 3DMAE模型
│   ├── rain_decoder.py # 降水反演模型
│   └── losses.py       # 损失函数
├── training/
│   ├── pretrain.py     # 预训练脚本
│   ├── finetune.py     # 微调脚本
│   └── trainer.py      # 通用训练器
├── configs/            # 配置文件
├── scripts/            # 工具脚本
├── outputs/            # 模型输出
└── README.md
```

## 技术要求
- Python 3.8+
- PyTorch 1.12+ with CUDA 11.3
- NVIDIA A800 80GB GPU（2张）
- 足够存储空间（数据文件约1.5TB）

## 时间估计
- 数据探索与加载器：1-2天
- 3DMAE实现与调试：2-3天
- 预训练（200 epoch）：3-5天（取决于数据量和优化）
- 降水反演实现：2-3天
- 微调训练（100 epoch）：2-3天
- 测试与文档：1-2天

## 风险与缓解
1. 数据质量问题：验证有效性标记，处理异常值
2. 显存限制：使用梯度累积，混合精度训练
3. 训练时间：从小的子集开始调试，逐步扩展到全数据
4. 收敛问题：仔细调整学习率调度和损失权重